# paragraph 
# Запустить
docker-compose -f docker-compose-local.yaml -p paragraph up -d

# Остановить 
docker-compose -f docker-compose-local.yaml -p paragraph down

# Посмотреть статус
docker-compose -f docker-compose-local.yaml -p paragraph ps







